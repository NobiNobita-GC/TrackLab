<UserControl x:Class="TrackLab.Client.View.CoolPlateModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrackLab.Client.View"
             mc:Ignorable="d" 
             Height="350" Width="500">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Cool Plate" FontSize="24" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="{Binding Module.Name}" FontSize="18"/>
        <TextBlock Grid.Row="2" Text="{Binding Module.Index, StringFormat=Index: {0}}"/>
        <TextBlock Grid.Row="3" Text="{Binding Module.State, StringFormat=State: {0}}"/>
        <TextBlock Grid.Row="4" Text="Temperature: -- °C"/>

    </Grid>
</UserControl>
