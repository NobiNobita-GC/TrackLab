<UserControl x:Class="UI.Control.ModuleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:convert="clr-namespace:UI.Converts">

    <UserControl.Resources>
        <convert:ModuleStatusConverter x:Key="ModuleStatusConverter" />
    </UserControl.Resources>

    <Border Margin="8"
            Padding="15"
            Background="{Binding ModuleEntity.State, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource ModuleStatusConverter}}"
            BorderBrush="{StaticResource PanelBorderBrush}"
            BorderThickness="1"
            CornerRadius="6">

        <StackPanel>

            <TextBlock FontSize="18"
                       FontWeight="SemiBold"
                       Text="{Binding ModuleEntity.Name, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            <TextBlock Margin="0,8,0,0"
                       Foreground="White"
                       Text="{Binding ModuleEntity.Type, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            <TextBlock Margin="0,6,0,0"
                       Foreground="White"
                       Text="{Binding ModuleEntity.State, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        </StackPanel>

    </Border>
</UserControl>
