<UserControl x:Class="TrackLabClient.View.Configuration.ShutDownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lan="clr-namespace:UI.Extension;assembly=UI">

    <Grid Background="{StaticResource PageBackgroundBrush}">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                <Button x:Name="Shutdown"
                        Width="200"
                        Height="100"
                        Margin="30"
                        lan:LanguageExtension.Text="Shut Down"
                        FontSize="22" />

                <Button Width="200"
                        Height="100"
                        Margin="30"
                        lan:LanguageExtension.Text="Restart"
                        FontSize="22"
                        x:Name="Restart"/>

            </StackPanel>

        </StackPanel>

    </Grid>

</UserControl>